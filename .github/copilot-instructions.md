<todos title="Make deneme toolbar match single-test" rule="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it.">
- [x] 1-plan: Compare toolbar and button styles between `goal-based/deneme/style.scss` and `goal-based/single-test/style.scss`; identify differences. 🔴
- [x] 2-apply: Edit `goal-based/deneme/style.scss` to match toolbar buttons/styling from `single-test`. 🔴
  _Replaced toolbar/button/menu CSS with single-test rules._
- [x] 3-verify: Quick visual/file check to ensure styles updated; run SASS compilation if necessary (user's environment compiles automatically). 🟡
  _Verified updated CSS and adjusted script to use .show and context-menu-item to match single-test._
- [x] 4-overlay: Replace deneme export overlay styles and export flow to match `single-test` exactly (DOM structure, class names, progress text and animations). 🔴
  _Replaced CSS block and updated _createExportOverlay/_exportToPDF to match single-test behavior._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
